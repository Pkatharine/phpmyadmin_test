const puppeteer = require("../node_modules/puppeteer");

const createTableNameField = "[name='table']";
const numberOfColomns = "[name='num_fields']";
const clickButtonGo = "input[type=submit]";
const createColomnName = "[id='field_0_1']";
// const buttonSaveColumn = "#page_content > form > fieldset > input[type=submit]:nth-child(2)";
const buttonSaveColumn = "input[type=submit]";
//create table
//Table name:
class CreateTable {
    async enterTableName(page, text) {
        await page.type(createTableNameField, text)
    }

    async enterColomnNumber(page, text) {
        await page.type(numberOfColomns, text)
    }

//finish creation of table
    async clickGo(page) {
        await page.click(clickButtonGo)


    }

    async enterColomnName(page, text) {
        await page.click(createColomnName);
        await page.type(createColomnName, text)
    }

    async clickSaveColomn(page) {
        await page.click(buttonSaveColumn);
    }
}

module.exports = CreateTable;

// class CreateColumn {
//     async enterColomnName(page, text) {
//         await page.click.type(createColomnName, text)
//     }
// }

